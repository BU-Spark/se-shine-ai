'use client'

import './BottomNavbar.css';
import Image from 'next/image';

export default function BottomNavbar() {
    return (
        <div className="router-fixed">
            <nav className="router">
            </nav>
        </div>
    );
}